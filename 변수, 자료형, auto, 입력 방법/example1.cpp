#include<iostream>
#include <string>

using namespace std;

/*
int main()
{
	// 변수명 작성
	int age = 0;
	// 변수를 보편화 된 최고하 방법 (C++ 11)
	int i{ 100 };				// int i = 100;
	short b = 0;
	bool flag = true;			// 1나옴

	// 변수의 주소 확인
	// 변수는 메모리 스택에 비 연속적으로 빈 공간을 찾아 들어감
	cout << "변수 age의 주소 : " << &age << "입니다." << endl;
	cout << "변수 i 의 주소 : " << &i << "입니다." << endl << endl;

	cout << "bool 값 : " << flag << endl;

	// 자료형의 크기 (sizeof(데이터 타입, 변수명))
	// to_string(정수) : 정수 값을 문자열 타입으로 변경시켜주는 string 헤더파일에 존재하는 메소드
	cout << "short형 자료형의 바이트 크기" + to_string(sizeof(short)) << endl;
	cout << "int형 자료형의 바이트 크기" + to_string(sizeof(int)) << endl;
	cout << "long  형 자료형의 바이트 크기" + to_string(sizeof(long)) << endl;
	cout << "long long형 자료형의 바이트 크기" + to_string(sizeof(long long)) << endl;
	cout << "bool형 자료형의 바이트 크기" + to_string(sizeof(bool)) << endl << endl;

	// string 타입 (문자열)
	string s1 = "안녕하세요";
	string s2 = "반갑습니다.";

	// 문자열 끼리는 + 연산자로 결합 가능
	string s3 = s1 + " " + s2 + "!!!";
	cout << s3 << endl;


	return 0;
}
*/