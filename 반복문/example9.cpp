// 더블루프에 대한 실습 / 구구단 출력

#include <iostream>
using namespace std;
/*
int main()
{
	// 곱셈 저장 변수
	int Gob = 0;

	// 외부 루프의 초기값은 딱 한번만 초기화
	for (int dan = 2; dan <= 9; dan++)
	{
		// 내부 루프의 초기값은 외부 루프가 루핑하는 만큼 초기화
		for (int cnt = 1; cnt <= 9; cnt++)
		{
			Gob = dan * cnt;					// 구구단 계산

			cout << dan << " * " << cnt << " = " << Gob << endl;
			cout << "외부 : " << dan << " /  " << "내부 : " << cnt << endl;
		}
		cout << endl;
	}
	return 0;
}
*/